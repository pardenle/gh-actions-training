name: Demo trigger events

on:
  # Webhook events
  push:
    branches: 
      - main
      - feat/* #is this real regex?
  pull_request:
    branches:
      - main
  issues:
    types: 
      - opened
      - closed
  # schedule events
  # manual events
 
jobs:
  Greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Greeting step
        run: echo "Hello World!"
